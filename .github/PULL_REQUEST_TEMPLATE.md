## Summary

<!-- What and why (1–3 bullets). -->

-

## Test plan

- [ ] `make check` (or `uv run ruff check . && uv run pytest -q`)
- [ ] Touched settings documented in `.env.example` / README if applicable
- [ ] Compose / Helm notes updated if deploy behavior changed
